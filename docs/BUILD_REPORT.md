# norad-war-simulator - Build Report

**Date:** April 1, 2026
**Version:** v0.5.0-alpha
**Godot:** 4.6.1.stable

## Build Status

| Platform | Status | Size | File |
|----------|--------|------|------|
| Linux x86_64 | ✅ SUCCESS | 68MB | `export/norad-war-simulator` |
| Windows x86_64 | ✅ SUCCESS | 100MB | `export/norad-war-simulator.exe` |
| macOS | ⏳ NEEDS TEMPLATE | — | Template not included in export pack |
| Web | ⏳ NOT BUILT | — | Not configured |
| Android | ⏳ NOT BUILT | — | Not configured |
| iOS | ⏳ NOT BUILT | — | Not configured |

## Build Commands

```bash
cd ~/stsgym-work/norad-war-simulator

# Linux
snap run godot-4 --headless --export-release "Linux/X11" export/norad-war-simulator

# Windows  
snap run godot-4 --headless --export-release "Windows Desktop" export/norad-war-simulator.exe

# macOS (requires macos.zip template extraction)
# First extract macos.zip from export templates:
unzip ~/snap/godot-4/common/.local/share/godot/export_templates/4.6.1.stable.mono/macos.zip
# Then build
snap run godot-4 --headless --export-release "macOS" export/norad-war-simulator.zip
```

## Export Templates Location

```
~/snap/godot-4/21/.local/share/godot/export_templates/4.6.1.stable.mono/
```

Templates installed:
- linux_release.x86_64 ✅
- windows_release.x86_64.exe ✅
- macos.zip (needs extraction for x86_64)

## Known Issues

1. **Audio warnings**: Some WAV files not imported correctly
2. **Music missing**: `menu_theme.ogg` not found
3. **macOS**: Standard template pack doesn't include x86_64 binary

## Files Generated

```
export/
├── norad-war-simulator      (68MB) - Linux executable
├── norad-war-simulator.exe  (100MB) - Windows executable
└── norad-war-simulator.pck  (3.8MB) - Game data pack
```

## Running the Game

```bash
# Linux
cd ~/stsgym-work/norad-war-simulator/export
./norad-war-simulator

# Windows (via Wine)
wine norad-war-simulator.exe
```

## Next Steps

1. Extract macOS templates for universal build
2. Take 10 screenshots for Steam store
3. Create 60-second trailer
4. Create GitHub release with binaries
5. Submit to Steam

---

*Generated by Lucky (OpenClaw Agent)*